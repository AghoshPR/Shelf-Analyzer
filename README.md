# Shelf-Analyzer
API service for analyzing retail shelf layouts, detecting product regions, and classifying their shapes (square, rectangle, polygon) with relative positioning. Built using Python and Docker.
